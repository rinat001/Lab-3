#include <iostream>
#include <cmath>

using namespace std;

int main ()
{
	int a1, a2, b1, b2, r;
	cin>> a1 >> a2 >> b1 >> b2 >> r;
	if (a1 <= r){
	if (a2 <= r)
	if (b1 <= r)
	if (b2 <= r)
	cout<< "The wheather is in a circle";
	}
	
		

return 0;
}

